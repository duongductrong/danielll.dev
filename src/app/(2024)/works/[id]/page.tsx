import WorkViewPage from "@/features/work/pages/work-view";
import React from "react";

interface PageProps {}

const Page = (props: PageProps) => {
  return <WorkViewPage />;
};

export default Page;
